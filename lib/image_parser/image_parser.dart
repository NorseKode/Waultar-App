import 'dart:io';

import 'package:flutter/material.dart';

void imageCapture() {
  final Directory _photoDir = new Directory('/lib/assets');

  // var imageList = _photoDir
  //     .listSync()
  //     .map((item) => item.path)
  //     .where((item) => item.endsWith(".png"))
  //     .toList(growable: false);

  // await for (var entity
  //     in _photoDir.list(recursive: true, followLinks: false)) {
  //   print(entity.path);
  // }
}
